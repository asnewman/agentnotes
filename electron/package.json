{
  "name": "agentnotes-electron",
  "version": "1.0.0",
  "description": "Electron app for AgentNotes",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/renderer.js --bundle --outfile=src/bundle.js --format=iife --platform=browser",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "@tiptap/core": "^2.1.0",
    "@tiptap/extension-highlight": "^2.1.0",
    "@tiptap/pm": "^2.1.0",
    "@tiptap/starter-kit": "^2.1.0",
    "electron": "^28.0.0",
    "gray-matter": "^4.0.3",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
